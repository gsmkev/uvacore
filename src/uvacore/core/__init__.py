"""Domain core primitives and contracts.

This package contains canonical domain data transfer objects, event schemas,
and abstract ports that define the boundary between `uvacore` and external
systems. Implementations live under `uvacore.infrastructure`.
"""
