"""Infrastructure implementations for adapters and event system.

Concrete, side-effecting integrations (e.g., platform SDKs, persistence,
transport) live under this namespace. These modules satisfy the abstract
ports defined in `uvacore.core.canonical.ports`.
"""